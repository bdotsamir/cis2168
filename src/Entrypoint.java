// Intellij isn't smart enough to only run the file you've selected.
// So this class is meant to provide an actual entrypoint to the class
// we are working on that day.
// (I could move to maven but that's a lot of work)
public class Entrypoint {
  public static void main(String[] args) {
    InClass124.main(null);
  }
}
